rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Public reads for all images
      // Protected writes (only authenticated users / admin dashboard can upload/delete images)
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
